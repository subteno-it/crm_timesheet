<?xml version="1.0" encoding="utf-8" ?>
<openerp>
    <data>

        <record model="ir.ui.view" id="view_crm_case_work_form">
            <field name="name">crm.case.case.work.form</field>
            <field name="model">crm.case</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="crm.crm_case-view"/>
            <field name="arch" type="xml">
                <xpath expr="/form/notebook" position="inside">
                    <page string="Timesheet">
                        <separator string="Analytic" colspan="4"/>
                        <group name="analytic" colspan="4">
                            <field name="analytic_account_id" domain="[('type','=','normal'),('partner_id','=',partner_id)]"/>
                            <newline/>
                        </group>
                        <field name="timesheet_ids" nolabel="1" mode="tree,form">
                            <tree string="Summary Work" editable="bottom">
                                <field name="date"/>
                                <field name="user_id"/>
                                <field name="hours" widget="float_time"/>
                                <field name="name"/>
                            </tree>
                            <form string="Summary Work">
                                <field name="name" colspan="4"/>
                                <field name="date"/>
                                <field name="user_id"/>
                                <field name="hours" widget="float_time"/>
                            </form>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>

    </data>
</openerp>
